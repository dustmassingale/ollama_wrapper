fastapi==0.100.0
uvicorn[standard]==0.22.0
requests==2.31.0
pydantic==1.10.12
python-dotenv==1.0.1

# Optional/back-end packages (uncomment if you plan to enable vector DB or reranking)
# chromadb provides a local vector store (Chroma)
# chromadb==0.3.27
# hnswlib is a fast in-memory k-NN index (install if using hnsw-based indexing)
# hnswlib==0.7.1
# faiss-cpu for production-grade nearest-neighbor search (Linux/macOS)
# faiss-cpu==1.7.4

# Dev / tooling (optional)
# black==24.1.0
# pytest==7.4.0
